# URL to your Oxidized instance (behind reverse proxy)
OXIDIZED_URL=https://oxidized.example.com

# Basic Auth credentials (used by the reverse proxy in front of Oxidized)
OXIDIZED_USER=admin
OXIDIZED_PASS=secret

# Port the MCP server listens on inside the container
MCP_PORT=8000
